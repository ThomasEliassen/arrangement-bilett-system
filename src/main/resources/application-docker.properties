# Docker-specific settings. Connection details are supplied by compose.yaml.
spring.mvc.servlet.path=/
server.servlet.context-path=/f21
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
